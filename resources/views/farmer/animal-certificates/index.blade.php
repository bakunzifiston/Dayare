<x-app-layout>
    <x-slot name="header"><div class="flex flex-wrap items-center justify-between gap-4 w-full"><h2 class="font-semibold text-xl text-slate-800">{{ __('Animal certificates') }}</h2><a href="{{ route('farmer.certificates.animal-certificates.create') }}" class="inline-flex items-center rounded-bucha bg-bucha-primary px-4 py-2 text-xs font-semibold uppercase tracking-widest text-white">{{ __('Issue certificate') }}</a></div></x-slot>
    <div class="max-w-7xl space-y-6">@include('farmer.animal-certificates.partials.nav')@if ($records->isEmpty())<p class="text-sm text-slate-500">{{ __('No animal certificates yet.') }}</p>@else<div class="overflow-hidden rounded-bucha border border-slate-200 bg-white shadow-sm"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-slate-600"><tr><th class="px-4 py-3">{{ __('Number') }}</th><th class="px-4 py-3">{{ __('Animal') }}</th><th class="px-4 py-3">{{ __('Type') }}</th><th class="px-4 py-3">{{ __('Status') }}</th><th class="px-4 py-3"></th></tr></thead><tbody class="divide-y divide-slate-100">@foreach ($records as $record)<tr><td class="px-4 py-3 font-mono text-xs">{{ $record->certificate_number }}</td><td class="px-4 py-3">{{ $record->animal?->animal_code }}</td><td class="px-4 py-3 capitalize">{{ str_replace('_', ' ', $record->certificate_type) }}</td><td class="px-4 py-3"><x-certificate-status-badge :status="$record->certificate_status" /></td><td class="px-4 py-3 text-right"><a href="{{ route('farmer.certificates.animal-certificates.show', $record) }}" class="text-bucha-primary hover:underline">{{ __('View') }}</a></td></tr>@endforeach</tbody></table></div><div>{{ $records->links() }}</div>@endif</div>
</x-app-layout>
