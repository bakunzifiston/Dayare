<x-app-layout>
    <x-slot name="header"><h2 class="font-semibold text-xl text-slate-800">{{ __('Certificate history & logs') }}</h2></x-slot>
    <div class="max-w-7xl space-y-6">@include('farmer.animal-certificates.partials.nav')<div class="overflow-hidden rounded-bucha border border-slate-200 bg-white shadow-sm"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-slate-600"><tr><th class="px-4 py-3">{{ __('Date') }}</th><th class="px-4 py-3">{{ __('Certificate') }}</th><th class="px-4 py-3">{{ __('Action') }}</th><th class="px-4 py-3">{{ __('IP') }}</th></tr></thead><tbody>@forelse ($logs as $log)<tr class="border-t border-slate-100"><td class="px-4 py-3">{{ $log->action_date?->toDateTimeString() }}</td><td class="px-4 py-3">{{ $log->certificate?->certificate_number }}</td><td class="px-4 py-3 capitalize">{{ str_replace('_', ' ', $log->action_type) }}</td><td class="px-4 py-3">{{ $log->ip_address ?: '—' }}</td></tr>@empty<tr><td colspan="4" class="px-4 py-8 text-center text-slate-500">{{ __('No certificate activity logged yet.') }}</td></tr>@endforelse</tbody></table></div><div>{{ $logs->links() }}</div></div>
</x-app-layout>
