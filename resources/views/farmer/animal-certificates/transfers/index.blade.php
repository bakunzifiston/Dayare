<x-app-layout>
    <x-slot name="header"><div class="flex justify-between gap-4 w-full"><h2 class="font-semibold text-xl text-slate-800">{{ __('Ownership transfers') }}</h2><a href="{{ route('farmer.certificates.ownership-transfers.create') }}" class="rounded-bucha bg-bucha-primary px-4 py-2 text-xs font-semibold uppercase tracking-widest text-white">{{ __('Record transfer') }}</a></div></x-slot>
    <div class="max-w-7xl space-y-6">@include('farmer.animal-certificates.partials.nav')@if ($records->isEmpty())<p class="text-sm text-slate-500">{{ __('No ownership transfers yet.') }}</p>@else<div class="overflow-hidden rounded-bucha border border-slate-200 bg-white shadow-sm"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-slate-600"><tr><th class="px-4 py-3">{{ __('Animal') }}</th><th class="px-4 py-3">{{ __('Previous owner') }}</th><th class="px-4 py-3">{{ __('New owner') }}</th><th class="px-4 py-3">{{ __('Date') }}</th></tr></thead><tbody>@foreach ($records as $record)<tr class="border-t border-slate-100"><td class="px-4 py-3">{{ $record->animal?->animal_code }}</td><td class="px-4 py-3">{{ $record->previous_owner }}</td><td class="px-4 py-3">{{ $record->new_owner }}</td><td class="px-4 py-3">{{ $record->transfer_date?->toDateString() }}</td></tr>@endforeach</tbody></table></div><div>{{ $records->links() }}</div>@endif</div>
</x-app-layout>
