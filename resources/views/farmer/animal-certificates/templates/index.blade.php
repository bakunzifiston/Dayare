<x-app-layout>
    <x-slot name="header"><div class="flex justify-between gap-4 w-full"><h2 class="font-semibold text-xl text-slate-800">{{ __('Certificate templates') }}</h2><a href="{{ route('farmer.certificates.templates.create') }}" class="rounded-bucha bg-bucha-primary px-4 py-2 text-xs font-semibold uppercase tracking-widest text-white">{{ __('Add template') }}</a></div></x-slot>
    <div class="max-w-7xl space-y-6">@include('farmer.animal-certificates.partials.nav')@if ($records->isEmpty())<p class="text-sm text-slate-500">{{ __('No templates yet.') }}</p>@else<div class="overflow-hidden rounded-bucha border border-slate-200 bg-white shadow-sm"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-slate-600"><tr><th class="px-4 py-3">{{ __('Name') }}</th><th class="px-4 py-3">{{ __('Type') }}</th><th class="px-4 py-3">{{ __('Status') }}</th></tr></thead><tbody>@foreach ($records as $record)<tr class="border-t border-slate-100"><td class="px-4 py-3">{{ $record->template_name }}</td><td class="px-4 py-3 capitalize">{{ str_replace('_', ' ', $record->certificate_type) }}</td><td class="px-4 py-3"><x-certificate-status-badge :status="$record->status" /></td></tr>@endforeach</tbody></table></div><div>{{ $records->links() }}</div>@endif</div>
</x-app-layout>
