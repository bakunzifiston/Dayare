<x-app-layout>
    <x-slot name="header"><h2 class="font-semibold text-xl text-slate-800">{{ __('Inventory batch') }}</h2></x-slot>
    <div class="max-w-4xl space-y-6">@include('farmer.feeding.partials.nav')<section class="rounded-bucha border border-slate-200 bg-white p-6 shadow-sm text-sm"><p class="text-xs uppercase tracking-wide text-slate-500">{{ $record->inventory_code }}</p><h3 class="mt-1 text-lg font-semibold text-slate-900">{{ $record->feedType?->feed_name }}</h3><p class="mt-2 text-slate-600">{{ __('Remaining') }}: {{ number_format((float) $record->quantity_remaining, 2) }} {{ $record->feedType?->unit }}</p><x-feed-status-badge :status="$record->status" class="mt-3" /></section><section class="rounded-bucha border border-slate-200 bg-white p-6 shadow-sm"><h3 class="text-sm font-semibold text-slate-900">{{ __('Movement history') }}</h3><ul class="mt-4 space-y-2 text-sm">@forelse ($record->movements as $movement)<li class="flex justify-between gap-3 border-b border-slate-100 pb-2"><span class="capitalize">{{ str_replace('_', ' ', $movement->movement_type) }}</span><span class="tabular-nums">{{ number_format((float) $movement->quantity_change, 2) }} → {{ number_format((float) $movement->balance_after, 2) }}</span></li>@empty<li class="text-slate-500">{{ __('No movements yet.') }}</li>@endforelse</ul></section></div>
</x-app-layout>
