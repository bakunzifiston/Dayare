<x-app-layout>
    <x-slot name="header"><div class="flex flex-wrap items-center justify-between gap-4 w-full"><h2 class="font-semibold text-xl text-slate-800">{{ __('Feeding schedules') }}</h2><a href="{{ route('farmer.feeding.schedules.create') }}" class="inline-flex items-center rounded-bucha bg-bucha-primary px-4 py-2 text-xs font-semibold uppercase tracking-widest text-white">{{ __('Add schedule') }}</a></div></x-slot>
    <div class="max-w-7xl space-y-6">@include('farmer.feeding.partials.nav')@if ($records->isEmpty())<p class="text-sm text-slate-500">{{ __('No feeding schedules yet.') }}</p>@else<div class="overflow-hidden rounded-bucha border border-slate-200 bg-white shadow-sm"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-slate-600"><tr><th class="px-4 py-3">{{ __('Schedule') }}</th><th class="px-4 py-3">{{ __('Target') }}</th><th class="px-4 py-3">{{ __('Feed') }}</th><th class="px-4 py-3">{{ __('Time') }}</th><th class="px-4 py-3">{{ __('Frequency') }}</th></tr></thead><tbody class="divide-y divide-slate-100">@foreach ($records as $record)<tr><td class="px-4 py-3">{{ $record->schedule_name }}</td><td class="px-4 py-3">{{ $record->animal?->animal_code ?: $record->livestock?->livestock_name }}</td><td class="px-4 py-3">{{ $record->feedType?->feed_name }}</td><td class="px-4 py-3">{{ $record->feeding_time }}</td><td class="px-4 py-3 capitalize">{{ str_replace('_', ' ', $record->feeding_frequency) }}</td></tr>@endforeach</tbody></table></div><div>{{ $records->links() }}</div>@endif</div>
</x-app-layout>
