<x-app-layout>
    <x-slot name="header"><div class="flex flex-wrap items-center justify-between gap-4 w-full"><h2 class="font-semibold text-xl text-slate-800">{{ __('Feed types') }}</h2><a href="{{ route('farmer.feeding.feed-types.create') }}" class="inline-flex items-center rounded-bucha bg-bucha-primary px-4 py-2 text-xs font-semibold uppercase tracking-widest text-white">{{ __('Add feed type') }}</a></div></x-slot>
    <div class="max-w-7xl space-y-6">@include('farmer.feeding.partials.nav')@if ($records->isEmpty())<p class="text-sm text-slate-500">{{ __('No feed types yet.') }}</p>@else<div class="overflow-hidden rounded-bucha border border-slate-200 bg-white shadow-sm"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-slate-600"><tr><th class="px-4 py-3">{{ __('Code') }}</th><th class="px-4 py-3">{{ __('Name') }}</th><th class="px-4 py-3">{{ __('Category') }}</th><th class="px-4 py-3">{{ __('Status') }}</th><th class="px-4 py-3"></th></tr></thead><tbody class="divide-y divide-slate-100">@foreach ($records as $record)<tr><td class="px-4 py-3 font-mono text-xs">{{ $record->feed_code }}</td><td class="px-4 py-3">{{ $record->feed_name }}</td><td class="px-4 py-3 capitalize">{{ str_replace('_', ' ', $record->feed_category) }}</td><td class="px-4 py-3"><x-feed-status-badge :status="$record->status" /></td><td class="px-4 py-3 text-right"><a href="{{ route('farmer.feeding.feed-types.show', $record) }}" class="text-bucha-primary hover:underline">{{ __('View') }}</a></td></tr>@endforeach</tbody></table></div><div>{{ $records->links() }}</div>@endif</div>
</x-app-layout>
