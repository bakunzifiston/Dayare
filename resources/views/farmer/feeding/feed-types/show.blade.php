<x-app-layout>
    <x-slot name="header"><h2 class="font-semibold text-xl text-slate-800">{{ __('Feed type') }}</h2></x-slot>
    <div class="max-w-4xl space-y-6">@include('farmer.feeding.partials.nav')<section class="rounded-bucha border border-slate-200 bg-white p-6 shadow-sm text-sm"><p class="text-xs uppercase tracking-wide text-slate-500">{{ $record->feed_code }}</p><h3 class="mt-1 text-lg font-semibold text-slate-900">{{ $record->feed_name }}</h3><dl class="mt-6 grid gap-3 sm:grid-cols-2"><div><dt class="text-slate-500">{{ __('Category') }}</dt><dd class="mt-1 capitalize">{{ str_replace('_', ' ', $record->feed_category) }}</dd></div><div><dt class="text-slate-500">{{ __('Form') }}</dt><dd class="mt-1 capitalize">{{ $record->feed_form }}</dd></div><div><dt class="text-slate-500">{{ __('Unit') }}</dt><dd class="mt-1">{{ $record->unit }}</dd></div><div><dt class="text-slate-500">{{ __('Status') }}</dt><dd class="mt-1"><x-feed-status-badge :status="$record->status" /></dd></div></dl></section><a href="{{ route('farmer.feeding.feed-types.edit', $record) }}" class="inline-flex items-center rounded-bucha border border-slate-300 px-4 py-2 text-sm">{{ __('Edit') }}</a></div>
</x-app-layout>
