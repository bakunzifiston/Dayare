<x-app-layout>
    <x-slot name="header"><h2 class="font-semibold text-xl text-slate-800">{{ __('Treatment record') }}</h2></x-slot>
    <div class="max-w-4xl space-y-6">@include('farmer.health.partials.nav')<section class="rounded-bucha border border-slate-200 bg-white p-6 shadow-sm text-sm"><div class="flex flex-wrap items-start justify-between gap-3"><div><p class="text-xs uppercase tracking-wide text-slate-500">{{ $record->treatment_code }}</p><h3 class="mt-1 text-lg font-semibold text-slate-900">{{ $record->disease_name ?: $record->medicine_name }}</h3></div><x-health-status-badge :status="$record->status" /></div><dl class="mt-6 grid gap-3 sm:grid-cols-2"><div><dt class="text-slate-500">{{ __('Animal') }}</dt><dd class="mt-1 font-medium">{{ $record->animal?->animal_code }}</dd></div><div><dt class="text-slate-500">{{ __('Start date') }}</dt><dd class="mt-1 font-medium">{{ $record->treatment_start_date?->toDateString() }}</dd></div><div><dt class="text-slate-500">{{ __('Follow-up date') }}</dt><dd class="mt-1 font-medium">{{ $record->follow_up_date?->toDateString() ?: '—' }}</dd></div><div class="sm:col-span-2"><dt class="text-slate-500">{{ __('Notes') }}</dt><dd class="mt-1">{{ $record->notes ?: '—' }}</dd></div></dl>@if ($record->attachmentUrl())<a href="{{ $record->attachmentUrl() }}" target="_blank" class="mt-4 inline-flex text-bucha-primary hover:underline">{{ __('Open attachment') }}</a>@endif</section><div class="flex flex-wrap gap-3"><a href="{{ route('farmer.health.treatments.edit', $record) }}" class="inline-flex items-center rounded-bucha border border-slate-300 px-4 py-2 text-sm">{{ __('Edit') }}</a></div></div>
</x-app-layout>
