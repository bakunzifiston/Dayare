<x-app-layout>
    <x-slot name="header"><h2 class="font-semibold text-xl text-slate-800">{{ __('Disease record') }}</h2></x-slot>
    <div class="max-w-4xl space-y-6">@include('farmer.health.partials.nav')<section class="rounded-bucha border border-slate-200 bg-white p-6 shadow-sm text-sm"><p class="text-xs uppercase tracking-wide text-slate-500">{{ $record->disease_code }}</p><h3 class="mt-1 text-lg font-semibold text-slate-900">{{ $record->disease_name }}</h3><dl class="mt-6 grid gap-3 sm:grid-cols-2"><div><dt class="text-slate-500">{{ __('Animal') }}</dt><dd class="mt-1 font-medium">{{ $record->animal?->animal_code }}</dd></div><div><dt class="text-slate-500">{{ __('Diagnosis date') }}</dt><dd class="mt-1 font-medium">{{ $record->diagnosis_date?->toDateString() }}</dd></div><div><dt class="text-slate-500">{{ __('Severity') }}</dt><dd class="mt-1"><x-health-status-badge :status="$record->severity_level" /></dd></div><div><dt class="text-slate-500">{{ __('Recovery') }}</dt><dd class="mt-1"><x-health-status-badge :status="$record->recovery_status" /></dd></div></dl></section><a href="{{ route('farmer.health.diseases.edit', $record) }}" class="inline-flex items-center rounded-bucha border border-slate-300 px-4 py-2 text-sm">{{ __('Edit') }}</a></div>
</x-app-layout>
