<x-app-layout>
    <x-slot name="header"><h2 class="font-semibold text-xl text-slate-800">{{ __('Sale animals') }}</h2></x-slot>
    <div class="max-w-7xl space-y-6">@include('farmer.sales.partials.nav')<form method="GET" class="rounded-bucha border border-slate-200 bg-white p-4 shadow-sm"><input type="search" name="q" value="{{ request('q') }}" placeholder="{{ __('Search animal code or tag...') }}" class="w-full rounded-lg border-gray-300 text-sm" /></form>@if ($records->isEmpty())<p class="text-sm text-slate-500">{{ __('No animal sale lines yet.') }}</p>@else<div class="overflow-hidden rounded-bucha border border-slate-200 bg-white shadow-sm"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-slate-600"><tr><th class="px-4 py-3">{{ __('Animal') }}</th><th class="px-4 py-3">{{ __('Sale') }}</th><th class="px-4 py-3">{{ __('Buyer') }}</th><th class="px-4 py-3">{{ __('Farm') }}</th><th class="px-4 py-3">{{ __('Price') }}</th><th class="px-4 py-3">{{ __('Condition') }}</th></tr></thead><tbody class="divide-y divide-slate-100">@foreach ($records as $record)<tr><td class="px-4 py-3">{{ $record->animal?->animal_code ?: '—' }}</td><td class="px-4 py-3"><a href="{{ route('farmer.sales.records.show', $record->sale) }}" class="text-bucha-primary hover:underline">{{ $record->sale?->sale_number }}</a></td><td class="px-4 py-3">{{ $record->sale?->buyer?->buyer_name }}</td><td class="px-4 py-3">{{ $record->sale?->farm?->name }}</td><td class="px-4 py-3 tabular-nums">{{ number_format($record->sale_price, 2) }}</td><td class="px-4 py-3 capitalize">{{ str_replace('_', ' ', $record->animal_condition) }}</td></tr>@endforeach</tbody></table></div><div>{{ $records->links() }}</div>@endif</div>
</x-app-layout>
