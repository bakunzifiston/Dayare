<x-app-layout>
    <x-slot name="header"><h2 class="font-semibold text-xl text-slate-800">{{ __('Sales documents') }}</h2></x-slot>
    <div class="max-w-7xl space-y-6">@include('farmer.sales.partials.nav')@if ($records->isEmpty())<p class="text-sm text-slate-500">{{ __('No sales documents yet.') }}</p>@else<div class="overflow-hidden rounded-bucha border border-slate-200 bg-white shadow-sm"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-slate-600"><tr><th class="px-4 py-3">{{ __('Document #') }}</th><th class="px-4 py-3">{{ __('Type') }}</th><th class="px-4 py-3">{{ __('Sale') }}</th><th class="px-4 py-3">{{ __('Generated') }}</th><th class="px-4 py-3"></th></tr></thead><tbody class="divide-y divide-slate-100">@foreach ($records as $record)<tr><td class="px-4 py-3 font-mono text-xs">{{ $record->document_number }}</td><td class="px-4 py-3 capitalize">{{ str_replace('_', ' ', $record->document_type) }}</td><td class="px-4 py-3">{{ $record->sale?->sale_number }}</td><td class="px-4 py-3">{{ $record->generated_at?->toDateTimeString() }}</td><td class="px-4 py-3 text-right"><a href="{{ route('farmer.sales.documents.download', $record) }}" class="text-bucha-primary hover:underline">{{ __('Download') }}</a></td></tr>@endforeach</tbody></table></div><div>{{ $records->links() }}</div>@endif</div>
</x-app-layout>
