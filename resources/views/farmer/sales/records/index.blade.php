<x-app-layout>
    <x-slot name="header"><div class="flex flex-wrap items-center justify-between gap-4 w-full"><h2 class="font-semibold text-xl text-slate-800">{{ __('Sales management') }}</h2><a href="{{ route('farmer.sales.records.create') }}" class="inline-flex items-center rounded-bucha bg-bucha-primary px-4 py-2 text-xs font-semibold uppercase tracking-widest text-white">{{ __('Create sale') }}</a></div></x-slot>
    <div class="max-w-7xl space-y-6">@include('farmer.sales.partials.nav')<form method="GET" class="grid gap-3 rounded-bucha border border-slate-200 bg-white p-4 shadow-sm md:grid-cols-5"><input type="search" name="q" value="{{ request('q') }}" placeholder="{{ __('Search sales...') }}" class="rounded-lg border-gray-300 text-sm md:col-span-2" /><select name="sale_type" class="rounded-lg border-gray-300 text-sm"><option value="">{{ __('All types') }}</option>@foreach (\App\Models\Sale::TYPES as $type)<option value="{{ $type }}" @selected(request('sale_type') === $type)>{{ __(ucwords(str_replace('_', ' ', $type))) }}</option>@endforeach</select><select name="sale_status" class="rounded-lg border-gray-300 text-sm"><option value="">{{ __('All statuses') }}</option>@foreach (\App\Models\Sale::STATUSES as $status)<option value="{{ $status }}" @selected(request('sale_status') === $status)>{{ __(ucfirst($status)) }}</option>@endforeach</select><button class="rounded-lg bg-slate-900 px-4 py-2 text-sm font-medium text-white">{{ __('Filter') }}</button></form>@if ($records->isEmpty())<p class="text-sm text-slate-500">{{ __('No sales yet.') }}</p>@else<div class="overflow-hidden rounded-bucha border border-slate-200 bg-white shadow-sm"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-slate-600"><tr><th class="px-4 py-3">{{ __('Sale #') }}</th><th class="px-4 py-3">{{ __('Buyer') }}</th><th class="px-4 py-3">{{ __('Farm') }}</th><th class="px-4 py-3">{{ __('Date') }}</th><th class="px-4 py-3">{{ __('Total') }}</th><th class="px-4 py-3">{{ __('Status') }}</th><th class="px-4 py-3">{{ __('Payment') }}</th><th class="px-4 py-3"></th></tr></thead><tbody class="divide-y divide-slate-100">@foreach ($records as $record)<tr><td class="px-4 py-3 font-mono text-xs">{{ $record->sale_number }}</td><td class="px-4 py-3">{{ $record->buyer?->buyer_name }}</td><td class="px-4 py-3">{{ $record->farm?->name }}</td><td class="px-4 py-3">{{ $record->sale_date?->toDateString() }}</td><td class="px-4 py-3 tabular-nums">{{ number_format($record->total_amount, 2) }} {{ $record->currency }}</td><td class="px-4 py-3"><x-sale-status-badge :status="$record->sale_status" /></td><td class="px-4 py-3"><x-payment-status-badge :status="$record->payment_status" /></td><td class="px-4 py-3 text-right"><a href="{{ route('farmer.sales.records.show', $record) }}" class="text-bucha-primary hover:underline">{{ __('View') }}</a></td></tr>@endforeach</tbody></table></div><div>{{ $records->links() }}</div>@endif</div>
</x-app-layout>
